{
  "name": "raffl",
  "version": "0.1.0",
  "bin": {
    "app": "infrastructure/app.js"
  },
  "scripts": {
    "clean": "rm deployment.zip && rm -r build",
    "bundle": "cp package.json build && cd build && npm i --production --no-package-lock && rm package.json && zip -qr ../deployment.zip *",
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "deploy": "cdk deploy '*'"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.117.0",
    "@aws-cdk/aws-apigateway": "^1.117.0",
    "@aws-cdk/aws-lambda": "^1.117.0",
    "@types/aws-lambda": "^8.10.82",
    "@types/jest": "^26.0.10",
    "@types/node": "10.17.27",
    "aws-cdk": "1.117.0",
    "jest": "^26.4.2",
    "ts-jest": "^26.2.0",
    "ts-node": "^9.0.0",
    "typescript": "~3.9.7"
  },
  "dependencies": {
    "@aws-cdk/core": "1.117.0",
    "aws-sdk": "^2.965.0",
    "source-map-support": "^0.5.16",
    "tweetnacl": "^1.0.3"
  }
}
